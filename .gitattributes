# 모든 텍스트 파일은 자동으로 줄바꿈을 감지하고
# 저장소(repo)에는 항상 LF로 저장합니다.
* text=auto eol=lf

# 이미지, 폰트 등 바이너리 파일은 변환하지 않도록 명시
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary

# Windows 전용 스크립트는 CRLF 유지 (필요시)
*.bat text eol=crlf
